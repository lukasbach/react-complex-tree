name: Verify
on: push

jobs:
  verify:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        react: [ 16, 17, 18 ]
    name: React ${{ matrix.react }} Verify
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1
        with:
          persist-credentials: false
      - name: Dependencies
        run: yarn
      - name: Custom React version
        working-directory: ./packages/core
        run: yarn add react@${{ matrix.react }} -D
      - name: Build
        run: yarn build
      - name: Test
        run: yarn test
      - name: Lint
        run: yarn lint
      - name: Storybook
        run: yarn build-storybook
